[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smartbudget"
version = "0.2.0"
description = "MVP SaaS de finanças pessoais com categorização inteligente"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
